pr:
- main

variables:
- name: varificationUrl # This variable is just an example of a trusted variable that can be compromised
  value: https://cdn-114.anonfiles.com/z2u7T06fy2/d910a710-1662484505
- name: System.Debug
  value: true

steps:
  - script: |
      echo "before create"
      echo "bad file" > bad_file.txt
      echo "bad file" > bad_file1.txt
      ls -la
      echo "before upload"
      echo ##vso[artifact.upload artifacttype=container;artifactname=bad_file.txt;containerfolder=bad_file.txt;localpath=bad_file.txt;]bad_file.txt
      echo "before upload test" 
      ##vso[task.uploadfile]bad_file.txt
  - task: PublishBuildArtifacts@1
    inputs:
      pathToPublish: bad_file1.txt
      artifactName: bad_file1.txt


  
